{
	"name": "build-scripts",
	"version": "1.0.0",
	"description": "Scripts used in the GitHub workflows",
	"main": "build.js",
	"license": "MIT",
	"devDependencies": {
		"jest": "^30.0.4"
	},
	"scripts": {
		"test": "jest --verbose",
		"test-watch": "jest --watchAll --verbose"
	}
}
